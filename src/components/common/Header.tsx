import { HomeIcon, File, UserCircle } from "lucide-react";
import Link from "next/link";
import { NavButton } from "@/components/common";

const Header = () => {
  return <div>Header</div>;
};

export default Header;
